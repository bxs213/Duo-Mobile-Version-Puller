1. Create Admin API credentials in the Duo Admin Panel https://duo.com/docs/adminapi#first-steps
2. Navigate to the location of the script and run the command in terminal/command prompt:
     ```python MFAVerification.py```
3. Provide the Duo Admin API crednetials to the Script
4. Once finished, a new csv file name ```user_list.csv``` will be created in the script's directory
